x <- 1:100 / 25
plot(x, x^2)

# plot(sin, -pi,2*pi)

# head(InsectSprays) # InsectSprays = built in DS in R
# head displayes first rows of DS

# boxplot(count ~ spray, data=InsectSprays)

# hist(islands) # built in DS

# barplot(VADeaths, legend = rownames(VADeaths))

# head(iris)